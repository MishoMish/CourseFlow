# ============================================
# FMI Courses Platform — Environment Variables
# ============================================
# Copy this file to .env and fill in ALL values marked with <...>
#
#   cp .env.example .env
#   nano .env
#
# Generate secrets with:
#   openssl rand -base64 32   (for passwords)
#   openssl rand -base64 64   (for JWT_SECRET)
#
# ⚠️  NEVER commit .env to git — it is in .gitignore

# ---- PostgreSQL ----
POSTGRES_USER=fmi_courses
POSTGRES_PASSWORD=<generate-a-strong-password>
POSTGRES_DB=fmi_courses
POSTGRES_HOST=db
POSTGRES_PORT=5432

# ---- Backend ----
NODE_ENV=production
PORT=3001
JWT_SECRET=<generate-a-64-char-secret>
JWT_EXPIRES_IN=7d
UPLOAD_MAX_SIZE_MB=50

# ---- Super Admin (created on first run) ----
SUPER_ADMIN_EMAIL=<your-email@fmi.uni-sofia.bg>
SUPER_ADMIN_PASSWORD=<generate-a-strong-password>
SUPER_ADMIN_NAME=Администратор

# ---- Frontend ----
VITE_API_URL=/api

# ---- Nginx ----
NGINX_PORT=8080
